<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Mani regelt das</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background: radial-gradient(circle at top, #ff3b3b, #b30000);
            color: white;
            text-align: center;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .headline {
            display: inline-block;
            background: #ffd400;
            color: #000;
            font-weight: 900;
            padding: 15px 25px;
            font-size: 2.2rem;
            transform: rotate(-2deg);
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .image {
            margin: 40px 0;
        }

        .image img {
            max-width: 100%;
            border-radius: 12px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.4);
        }

        .claim {
            font-size: 1.8rem;
            font-weight: bold;
            margin: 30px 0;
        }

        .cta {
            display: inline-block;
            background: #ffd400;
            color: #000;
            font-weight: 900;
            font-size: 1.6rem;
            padding: 15px 30px;
            text-decoration: none;
            margin-top: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .cta:hover {
            background: #ffea61;
        }

        .footer {
            margin-top: 60px;
            font-size: 0.9rem;
            opacity: 0.8;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="headline">MANI REGELT DAS!</div>

    <div class="image">
        <img src="bild.jpg" alt="Mani regelt das">
    </div>

    <div class="claim">Verlass dich drauf.</div>

    <a class="cta" href="#">maniregeltdas.ch</a>

    <div class="footer">
        Fake Demo-Seite · Kein echtes Angebot · Just for fun
    </div>
</div>

</body>
</html>
